import { Box } from "@chakra-ui/react";

export default function RepoCard({
  repoName,
  description,
  primaryLanguage,
  numStars,
  numForks,
}: {
  repoName: string;
  description: string;
  primaryLanguage: string;
  numStars: number;
  numForks: number;
}) {
  return <></>;
}
